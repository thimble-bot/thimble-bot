// Here you can define custom event handlers.
// Only define stuff here if it's an event that is not defined in the
// main entry point of the bot (index.js).

// See: https://anidiots.guide/understanding/events-and-handlers

module.exports = function (client) {
  // for example:

  // client.on('guildMemberAdd', e => {
  //   console.log(e);
  //   // or:
  //   require('./something/submodule.js')(client, e);
  //   // depending on your setup
  // });
};
